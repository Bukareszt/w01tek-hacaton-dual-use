---
phase: 01-isolated-rai-environment
plan: 04
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - experiments/wojtek_rai_v1/tests/test_isolation_boundary.py
  - experiments/wojtek_rai_v1/tests/test_pinned_versions.py
  - experiments/wojtek_rai_v1/tests/test_compose_override.py
  - experiments/wojtek_rai_v1/tests/test_model_free_guard.py
  - experiments/wojtek_rai_v1/README.md
autonomous: true
requirements: [FOUND-01, FOUND-02, FOUND-03, FOUND-06]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A file outside experiments/wojtek_rai_v1/ that references the experiment by name makes run.sh test fail; the same reference inside the experiment directory does not — the directory boundary is the tested boundary"
    - "run.sh test fails if any package under ros/src/ or any path ros/deploy.sh rsyncs could reach the experiment"
    - "run.sh test fails if pyproject.toml stops pinning rai-core or rai-whoami with an exact equality specifier, or if uv.lock is missing, empty or unparseable"
    - "run.sh test fails if docker/compose.override.yaml redeclares the wojtek_robot service's DDS or RMW variables, or stops declaring the experiment bind mount"
    - "run.sh test exits 0 with no LLM API key set, no ROS 2 daemon running and no GPU present, and the suite reports zero skipped tests"
    - "Every guard test in the suite is proven fail-first: each one is shown to go red against a deliberately introduced violation before being accepted as green"
  prohibitions:
    - "The experiment must never become a dependency of wojtek_bringup or of any package under ros/src/, and must never be reachable by ros/deploy.sh"
    - "run.sh test must not report success on a suite that silently skipped its guard tests, scanned an empty file set, or collected zero tests"
    - "No file under ros/docker/, ros/sim.sh, ros/dev.sh or ros/src/ may be edited to make the experiment work"
  artifacts:
    - path: "experiments/wojtek_rai_v1/tests/test_isolation_boundary.py"
      provides: "FOUND-01 import-boundary and deploy-reachability guard"
    - path: "experiments/wojtek_rai_v1/tests/test_pinned_versions.py"
      provides: "FOUND-02 exact-pin and lockfile guard"
    - path: "experiments/wojtek_rai_v1/tests/test_compose_override.py"
      provides: "FOUND-03 static half — override adds the mount and inherits DDS settings"
    - path: "experiments/wojtek_rai_v1/tests/test_model_free_guard.py"
      provides: "FOUND-06 self-referential guard — the suite needs no LLM key, ROS runtime or GPU"
    - path: "experiments/wojtek_rai_v1/README.md"
      provides: "Status line, layout table, isolation rules, external-touch table"
      contains: "## Status"
  key_links:
    - from: "experiments/wojtek_rai_v1/tests/test_isolation_boundary.py"
      to: "ros/src"
      via: "grep for the experiment name across ros/src and training, asserting no hit"
      pattern: "ros/src"
    - from: "experiments/wojtek_rai_v1/tests/test_compose_override.py"
      to: "experiments/wojtek_rai_v1/docker/compose.override.yaml"
      via: "yaml.safe_load of the override, asserting the volume entry and the absence of a redeclared variables block"
      pattern: "compose.override.yaml"
---

<objective>
**As a** Wojtek developer, **I want to** `run.sh test` and have it fail the moment the experiment
starts leaking outside its directory or its pins start drifting, **so that** the isolation contract
is enforced by a command rather than by memory.

This slice turns the four success criteria that are currently properties of the tree into
executable assertions, and writes the README that states the contract in prose for a human reader.

Purpose: FOUND-01's "a test enforces the import boundary", FOUND-02's lockfile guarantee,
FOUND-03's static half, and FOUND-06's self-referential model-free guarantee.
Output: four guard tests, each demonstrated fail-first, plus the experiment README.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-isolated-rai-environment/01-CONTEXT.md
@.planning/phases/01-isolated-rai-environment/01-RESEARCH.md
@.planning/phases/01-isolated-rai-environment/01-PATTERNS.md
@.planning/phases/01-isolated-rai-environment/01-VALIDATION.md
@.planning/phases/01-isolated-rai-environment/01-01-SUMMARY.md
@.planning/phases/01-isolated-rai-environment/01-02-SUMMARY.md
@CLAUDE.md
@experiments/autonomous_architecture_ros2_v1/README.md
</context>

## Artifacts this phase produces

Created by this plan:

- `experiments/wojtek_rai_v1/tests/test_isolation_boundary.py`
- `experiments/wojtek_rai_v1/tests/test_pinned_versions.py`
- `experiments/wojtek_rai_v1/tests/test_compose_override.py`
- `experiments/wojtek_rai_v1/tests/test_model_free_guard.py`
- `experiments/wojtek_rai_v1/README.md`

No new `run.sh` subcommand and no new Python package symbol. The tests consume the `repo_root` and
`experiment_dir` fixtures created in plan 01-01.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Isolation boundary guard — nothing outside imports it, ros/deploy.sh cannot ship it</name>
  <files>
    experiments/wojtek_rai_v1/tests/test_isolation_boundary.py
  </files>
  <read_first>
    - .planning/phases/01-isolated-rai-environment/01-RESEARCH.md § "Code Examples" → the drafted `test_isolation_boundary.py` (its repository-root discovery, the `grep -rl` subprocess call over the production trees, and the assertion that no experiment-named package exists under `ros/src`) — copy this draft and extend it as described below
    - training/tests/unit/test_job_scripts.py (the full 131 lines — module-level compiled regex constants each with an explanatory comment, a `Path`-returning discovery helper, `pytest.mark.parametrize` over that helper, and assertions carrying the offending file name and matched values)
    - experiments/autonomous_architecture_ros2_v1/README.md § "The isolation rules this experiment follows" (rules 1 through 5 — the five properties this test makes executable)
    - ros/deploy.sh (the rsync invocation that copies `ros/src/` to the robot and the `--packages-up-to wojtek_bringup` colcon call — the exact reachability path this test closes)
    - experiments/wojtek_rai_v1/tests/conftest.py (the `repo_root` and `experiment_dir` fixtures)
    - experiments/wojtek_rai_v1/tests/test_repos_pin.py (the overlay-location assertion already written in plan 01-02 — do not duplicate it, extend the coverage instead)
  </read_first>
  <behavior>
    - The test fails when any file under `ros/`, `training/` or the repository's top-level shell scripts references the experiment directory name
    - The test passes when the same reference appears inside `experiments/wojtek_rai_v1/` — the directory itself is the boundary, and one step inside it is legal while one step outside is not
    - The test fails when a path under `ros/src/` matches the experiment name or `rai`
    - The test fails when the rsync source list in `ros/deploy.sh` names anything outside `ros/`
    - The test fails loudly, rather than passing, when its own search finds no files to search — an empty scan set is an error, not a clean result
  </behavior>
  <action>
    Write `experiments/wojtek_rai_v1/tests/test_isolation_boundary.py` from the draft in the research
    document, extended in three ways.

    First, widen the scanned trees. The draft greps `ros/src` and `training`; also cover the
    repository's top-level shell entry points and the rest of `ros/`, since a reference from
    `ros/deploy.sh` or `ros/sim.sh` would be an isolation violation even though neither lives under
    `ros/src`. Exclude `.planning/` from the scan — planning documents legitimately name the
    experiment — and say so in a comment so the exclusion is deliberate rather than accidental.

    Second, make the boundary itself the tested property, not just the absence of hits. Add a test
    that writes a reference to the experiment name into a temporary file inside the experiment
    directory and asserts the scan still passes, and one that does the same one directory level
    outside and asserts the scan fails. That pair is what turns "nothing outside imports it" from a
    snapshot into a boundary contract. Use `tmp_path`-based fixtures or create-and-remove within a
    `try`/`finally` so the working tree is left clean either way.

    Third, close the deploy-reachability path explicitly. Read `ros/deploy.sh`, locate the rsync
    source arguments, and assert every one of them resolves inside `ros/`. This is the assertion
    that makes "`ros/deploy.sh` cannot ship it" checkable rather than asserted, and it will also
    catch a future change to the deployment script that widens its reach.

    Add the empty-scan guard: if the discovery step returns no files, fail with a message saying the
    scan found nothing to scan. A guard test that silently scans an empty set is worse than no test.

    Follow the structural conventions of the training repository's job-script guard throughout:
    module-level compiled regex constants with a comment above each stating exactly what it matches
    and why, a `Path`-returning discovery helper, and every assertion carrying an f-string with the
    offending file and the matched value. Open the file with a docstring stating what this
    repository cares about here — the five isolation rules — and what this test deliberately does
    not check.

    Before accepting the test as green, prove it fail-first for each of the three properties:
    introduce a reference outside the experiment, a stray path under `ros/src`, and a widened rsync
    source; confirm the suite goes red for each; revert each.
  </action>
  <verify>
    <automated>./experiments/wojtek_rai_v1/run.sh test -k isolation_boundary</automated>
  </verify>
  <acceptance_criteria>
    - `./experiments/wojtek_rai_v1/run.sh test -k isolation_boundary` exits 0 and reports at least 5 passing tests and 0 skipped
    - `grep -c 'deploy.sh' experiments/wojtek_rai_v1/tests/test_isolation_boundary.py` returns at least 1
    - `grep -c 'planning' experiments/wojtek_rai_v1/tests/test_isolation_boundary.py` returns at least 1
    - Introducing the string `wojtek_rai_v1` into a new file under `training/` makes `./experiments/wojtek_rai_v1/run.sh test` exit non-zero — demonstrate, then remove the file
    - `./experiments/wojtek_rai_v1/run.sh test` exits 0 on the reverted tree
    - `git status --porcelain` is empty after the fail-first demonstrations
  </acceptance_criteria>
  <done>
    The import boundary and the deployment-reachability path are both enforced by a test that has
    been demonstrated to go red on a real violation and green again after revert.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pinned-version and compose-override guards</name>
  <files>
    experiments/wojtek_rai_v1/tests/test_pinned_versions.py,
    experiments/wojtek_rai_v1/tests/test_compose_override.py,
    experiments/wojtek_rai_v1/tests/test_model_free_guard.py
  </files>
  <read_first>
    - .planning/phases/01-isolated-rai-environment/01-PATTERNS.md § "test_pinned_versions.py and test_compose_override.py" (the extrapolation instructions: `tomllib` static parse of the project file and the lockfile; `yaml.safe_load` of the override asserting no redeclared variables block under the service and a declared bind mount)
    - .planning/phases/01-isolated-rai-environment/01-RESEARCH.md § "Common Pitfalls" Pitfall 1 (the six unconstrained transitives the lockfile exists to pin) and Pitfall 4 (why redeclaring the container's DDS variables in the override would break discovery)
    - .planning/phases/01-isolated-rai-environment/01-VALIDATION.md (the per-task verification map — the rows these three tests satisfy)
    - experiments/wojtek_rai_v1/pyproject.toml and experiments/wojtek_rai_v1/uv.lock (the files under test, from plan 01-01)
    - experiments/wojtek_rai_v1/docker/compose.override.yaml (the file under test, from plan 01-01)
    - ros/docker/compose.yaml (the base service whose variables the override must inherit rather than restate)
    - training/tests/unit/test_job_scripts.py (structural conventions, as in Task 1)
  </read_first>
  <behavior>
    - `test_pinned_versions` fails when the project file stops pinning `rai-core` or `rai-whoami` with an exact equality specifier, or pins a version other than the two this milestone locked
    - `test_pinned_versions` fails when the lockfile is missing, empty, or does not parse
    - `test_pinned_versions` fails when the lockfile contains no pinned entry for `langgraph-prebuilt` — the single package whose drift broke callers upstream
    - `test_compose_override` fails when the override declares a service-level variables mapping for the container service
    - `test_compose_override` fails when the override stops declaring a volume whose target is the experiment's path inside the container
    - `test_compose_override` fails when the override's volume source is not written relative to the base compose file's directory
    - `test_model_free_guard` fails when any file in the test suite imports a ROS 2 client library, a RAI module, or a model SDK at module scope
    - `test_model_free_guard` fails when the collected test count is zero
  </behavior>
  <action>
    Write the three remaining guard tests. All three are static parses: no live process, no network,
    no fixtures beyond `conftest.py`'s path fixtures.

    `test_pinned_versions.py` — parse the project file with `tomllib` and assert `rai-core` and
    `rai-whoami` each appear as a direct dependency with an exact equality specifier, at the two
    versions this milestone locked. Assert the reverse for RAI's transitives: no direct dependency
    entry whose name starts with the LangChain or LangGraph distribution prefixes carries a version
    specifier, because per the pinning decision those stay at RAI's own ranges and are pinned only
    by the lockfile. Then parse the lockfile with `tomllib`, assert it is non-empty, and assert it
    contains a resolved version entry for `langgraph-prebuilt` and for at least four of the other
    five unconstrained distributions named in the research pitfall. Failing on a missing lockfile
    entry is the point: the lockfile is the only real pin those packages have.

    `test_compose_override.py` — load the override with `yaml.safe_load`. Assert it defines exactly
    one service and that the service key matches the container service name in the base compose
    file, read from `ros/docker/compose.yaml` rather than hardcoded, so a rename in the base file
    surfaces here. Assert the service mapping declares a volumes list and does not declare a
    service-level variables mapping — the DDS domain, middleware implementation and middleware
    configuration path are inherited unmodified from the base service, and restating them is the
    documented way to silently land on the wrong DDS domain. Assert exactly one volume entry, whose
    target is the experiment's path inside the container and whose source is a relative path that,
    resolved against the directory containing the base compose file, is the experiment directory —
    this is the assertion that catches the easy mistake of writing a source relative to the override
    file's own location, which would mount the wrong directory with no error. Compute the expected
    source by resolving it the way Compose does rather than by string comparison.

    `test_model_free_guard.py` — the self-referential guard. Walk every Python file in the test
    directory, parse each with the standard library's abstract syntax tree module, and assert no
    module-scope import names a ROS 2 client library, a RAI package, or a model vendor SDK. Function-
    local imports are allowed and are the convention the experiment's own modules follow. Then assert
    the suite is non-vacuous: collect the test files and fail if there are fewer than the number this
    phase creates. Add a check that no test in the suite is marked skip unconditionally. Open the file
    with a docstring stating the contract in one line: this suite needs no model key, no ROS runtime
    and no GPU.

    Prove each of the three fail-first before accepting them: relax a pin, add a variables mapping to
    the override, and add a module-scope ROS import to a test file; confirm the suite goes red for
    each; revert each.
  </action>
  <verify>
    <automated>./experiments/wojtek_rai_v1/run.sh test</automated>
  </verify>
  <acceptance_criteria>
    - `./experiments/wojtek_rai_v1/run.sh test` exits 0 and reports 0 skipped tests
    - `./experiments/wojtek_rai_v1/run.sh test -k "pinned_versions or compose_override or model_free"` exits 0 with at least 8 passing tests
    - `grep -c 'langgraph-prebuilt' experiments/wojtek_rai_v1/tests/test_pinned_versions.py` returns at least 1
    - `grep -c 'ast' experiments/wojtek_rai_v1/tests/test_model_free_guard.py` returns at least 1
    - `grep -vE '^[[:space:]]*#' experiments/wojtek_rai_v1/tests/test_compose_override.py | grep -c 'compose.yaml'` returns at least 1
    - Relaxing the `rai-core` pin in `experiments/wojtek_rai_v1/pyproject.toml` makes the suite exit non-zero — demonstrate, then revert
    - `env -u OPENAI_API_KEY -u ANTHROPIC_API_KEY -u GOOGLE_API_KEY ./experiments/wojtek_rai_v1/run.sh test` exits 0
    - `git status --porcelain` is empty after the fail-first demonstrations
  </acceptance_criteria>
  <done>
    Version pinning, the compose override's shape, and the suite's own model-free contract are each
    enforced by a test demonstrated to go red on a real violation.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write the experiment README stating the isolation contract</name>
  <files>
    experiments/wojtek_rai_v1/README.md
  </files>
  <read_first>
    - experiments/autonomous_architecture_ros2_v1/README.md (the full file — the status block at lines 1-6, the layout table at lines 22-37, the five isolation rules at lines 39-63, and the "what this experiment does change outside itself" table at lines 64-74; this is the structural template)
    - .planning/phases/01-isolated-rai-environment/01-CONTEXT.md (D-13 for the layout, and the discretion item on README status wording)
    - .planning/phases/01-isolated-rai-environment/01-SKELETON.md (the architectural decisions and the out-of-scope list this README summarises for a human reader)
    - experiments/wojtek_rai_v1/run.sh (the actual implemented subcommands, so the README's usage section matches reality rather than the plan)
    - CLAUDE.md § "Repository map and boundaries" (the `experiments/` rule and the public-repository rule)
  </read_first>
  <action>
    Write `experiments/wojtek_rai_v1/README.md` following the sibling experiment's structure
    section for section.

    Open with the title and a status block in the sibling's shape and tone: experimental, not
    production, not on the robot; nothing here is deployed by the deployment script; no package here
    is a dependency of the robot bringup package; treat every interface in this directory as
    unstable. Give the block a `## Status` heading so it is greppable.

    Add a one-paragraph scope statement saying what this experiment is at the end of Phase 1 — a
    pinned RAI environment that joins the running simulation's ROS 2 graph and lists its topics —
    and, just as important, what it is not yet: no agent, no tools, no chat, no model call. Point at
    the roadmap for what lands in Phases 2 through 5.

    Add a usage section listing every implemented `run.sh` subcommand with one line each, in the
    order a new developer runs them, and stating explicitly which of them need the container running
    and which do not. Record the container lifecycle decision taken at the plan 01-01 checkpoint,
    because that is the thing a returning developer will get wrong first.

    Add a layout table mapping each path in the directory to one line of purpose, matching the layout
    the phase actually produced.

    Add an "isolation rules this experiment follows" section adapted from the sibling's rules 1
    through 5: nothing outside this directory imports anything inside it; no shared build or test
    configuration knows about the experiment, and the base container configuration and the
    simulation and development entry points are extended by this experiment's own override file
    rather than edited; the ROS packages live in this experiment's own overlay rather than the
    production source tree, so the deployment script's rsync cannot ship them; behaviour needed from
    existing code is added by extension, not edit; one implementation of shared logic. For each rule,
    name the test in `tests/` that enforces it, so a reader can see the rule is executable.

    Add a "what this experiment changes outside itself" table in the sibling's shape. It has exactly
    one row: the repository-root environment template, which gains placeholder names for the vendor
    and tracing credentials, with the justification that this project keeps a single secrets file at
    the root rather than one per experiment.

    Close with a short section stating that credentials come only from the gitignored root
    environment file, that the committed vendor configuration carries model names only, and that
    nothing in this directory may name private infrastructure. Do not write any credential,
    hostname, address, login or alias anywhere in the file — refer to the second machine only as the
    remote GPU dev box.
  </action>
  <verify>
    <automated>./experiments/wojtek_rai_v1/run.sh test</automated>
    <human-check>
      Read the README top to bottom as a developer who has never seen the experiment and confirm
      that following it in order — install, test, then the topic-discovery command — matches what
      `run.sh` actually implements.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `experiments/wojtek_rai_v1/README.md` contains the headings `## Status`, a usage section, a layout table, an isolation-rules section and a table of external changes
    - `grep -c 'EXPERIMENTAL' experiments/wojtek_rai_v1/README.md` returns at least 1
    - Every subcommand printed by `./experiments/wojtek_rai_v1/run.sh` with no argument appears in the README
    - `grep -cE '([0-9]{1,3}\.){3}[0-9]{1,3}' experiments/wojtek_rai_v1/README.md` returns 0
    - `grep -cE '[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+' experiments/wojtek_rai_v1/README.md` returns 0
    - `./experiments/wojtek_rai_v1/run.sh test` exits 0 with the secret and identity scans from plan 01-03 covering the new README
  </acceptance_criteria>
  <done>
    The README states the experiment's status, scope, usage, layout and isolation contract, names
    the test enforcing each isolation rule, and contains no private-infrastructure identifier.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| experiment tree → production trees (`ros/`, `training/`) | A single import or reference across this line makes the experiment undeletable and potentially deployable |
| `ros/src/` → the physical robot | The deployment script rsyncs this tree and builds it on the robot |
| the test suite → the developer's confidence | A vacuous green suite is worse than no suite, because it is trusted |
| this repository → the public internet | The README is published; anything named in it is published |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-16 | Elevation of Privilege | A future change importing the experiment from a production package, giving experimental code robot-deployment reach | high | mitigate | `test_isolation_boundary.py` scans `ros/`, `training/` and the top-level scripts for the experiment name, asserts the deployment script's rsync sources all resolve inside `ros/`, and is proven fail-first against a real violation |
| T-01-17 | Tampering | A guard test passing vacuously — an empty scan set, a wrong search root, or zero collected tests | high | mitigate | Every scan asserts its discovery step returned a non-empty set; `test_model_free_guard.py` fails on a below-threshold collected-test count and on unconditional skips; each guard is demonstrated red-then-green before acceptance |
| T-01-18 | Tampering | Silent dependency drift after a future re-resolve | medium | mitigate | `test_pinned_versions.py` asserts exact equality pins on the two direct RAI packages and the presence of resolved lockfile entries for the six unconstrained transitives |
| T-01-19 | Spoofing | The compose override silently mounting the wrong host directory because the source path was written relative to the wrong base | medium | mitigate | `test_compose_override.py` resolves the source the way Compose resolves it — against the base compose file's directory — and compares against the experiment directory rather than doing a string match |
| T-01-20 | Information Disclosure | The README naming the remote GPU dev box's host, address, login or alias | high | mitigate | The README refers to the second machine only by role; address-shaped and address-at-host-shaped patterns are grep-asserted absent, and the identity scan from plan 01-03 covers the file |
</threat_model>

<verification>
1. `./experiments/wojtek_rai_v1/run.sh test` exits 0 with zero skipped tests and no LLM key, no ROS
   2 daemon and no GPU.
2. Each of the four guard tests has been demonstrated to fail against a deliberately introduced
   violation and to pass again after revert, with `git status --porcelain` empty afterwards.
3. The README's usage section matches `run.sh`'s actual subcommand list.
4. No address, login or alias appears anywhere in the README.
</verification>

<success_criteria>
- Four guard tests exist, all green, all proven fail-first
- The suite reports zero skipped tests and a non-vacuous collected count
- The README states the status, scope, usage, layout, isolation rules and the single external touch
- Each isolation rule in the README names the test that enforces it
</success_criteria>

<output>
Create `.planning/phases/01-isolated-rai-environment/01-04-SUMMARY.md` when done.
Record in it: for each of the four guard tests, the exact violation used to prove it fail-first and
the observed red-then-green result; the final collected test count; and confirmation that
`git status --porcelain` was empty after every demonstration.
</output>
